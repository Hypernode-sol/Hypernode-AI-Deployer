# Inference container for serving fine-tuned models
FROM hypernode/ai-deployer-base:latest

WORKDIR /srv/infer
COPY . /srv/infer

ENV PYTHONUNBUFFERED=1
CMD ["uvicorn", "api.main:app", "--host", "0.0.0.0", "--port", "8080"]
